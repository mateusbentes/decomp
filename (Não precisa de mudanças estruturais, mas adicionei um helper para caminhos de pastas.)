// ... (código existente permanece igual) ...

public static string GetOutputPath(string inputPath, string outputPath = null)
{
    if (string.IsNullOrEmpty(outputPath))
    {
        return Path.GetDirectoryName(inputPath);
    }
    Directory.CreateDirectory(outputPath);
    return outputPath;
}
